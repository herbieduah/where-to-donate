import React from "react";

const MentalHealth = ({ title, pageClass }) => {
  return (
    <div className={pageClass}>
      <h1>{title}</h1>
      <div className="container">
        <strong>Coming Soon</strong>
      </div>
    </div>
  );
};

export default MentalHealth;
